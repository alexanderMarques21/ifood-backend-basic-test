hazelcast:
  network:
    join:
      multicast:
        enabled: true
  map:
    weathers:
      time-to-live-seconds: 360
      max-idle-seconds: 20
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 5000